services:
  deepphe-visualizer-v2:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: deepphe-visualizer-v2
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
    healthcheck:
      test: [ "CMD", "node", "-e", "require('http').get('http://localhost:3000', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - deepphe-network
    labels:
      - "com.deepphe.description=DeepPhe Visualizer v2"
      - "com.deepphe.version=2.0.0"

  # Optional: Nginx reverse proxy
  nginx:
    image: nginx:alpine
    container_name: deepphe-visualizer-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - deepphe-visualizer-v2
    restart: unless-stopped
    networks:
      - deepphe-network
    profiles:
      - with-nginx

networks:
  deepphe-network:
    driver: bridge
